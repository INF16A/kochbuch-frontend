import { DifficultyPipe } from './difficulty.pipe';

describe('DifficultyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
